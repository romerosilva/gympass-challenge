class LogEntry:
    def __init(time, code, pilot, lap, lapTime, avgSpeed):
        self.time = time
        self.code = code
        self.pilot = pilot
        self.lap = lap
        self.lapTime = lapTime
        self.avgSpeed = avgSpeed
